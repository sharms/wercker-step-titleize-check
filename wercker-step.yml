name: wercker-step-titleize-check
version: 1.0.2
description: |
    Downloads the titleize gem and runs it against Markdown files.
type: build
properties:
    version:
        type: string
        default: "1.0.2"
    basedir:
        type: string
        default: ""
        required: false
    verbose:
        type: string
        default: "--verbose"
        required: false
